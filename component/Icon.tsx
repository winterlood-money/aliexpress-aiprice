import React, { useState, useEffect } from "react";
type Props = {};
const Icon = (type) => {
    if (type === "left-arrow") {
        return <></>;
    }
    return <div></div>;
};

export default Icon;
